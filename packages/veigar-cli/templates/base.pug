template(name='$_TPL')
  block(tt:for='{{root.children}}' tt:key='{{id}}')
    template(is="{{'$_' + item.type}}" data='{{item: item}}')/

include ./component.pug

include ./customComponent.pug

template(name="$_rawText") {{item.text}}